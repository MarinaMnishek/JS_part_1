1. Для практикума из занятия 7 продумать, где можно применить замыкания.
Мне кажется, замыкания можно использовать при различных проверках(на еду, на бомбы...)


2. Не выполняя кода, ответить, что выведет браузер и почему:


if (!("a" in window)) {
    var a = 1;
}
alert(a);

//undefined
window - это объект. Если что-то хотим из негополучить,
то надо использовать методы и свойства объекта 




var b = function a(x) {
    x && a(--x);
};
alert(a);

//пусто
функция a() неопределена





function a(x) {
    return x * 2;
}
var a;
alert(a);

//a(x) { return x * 2;}
Я ответила, что будет пусто. Почему такой вывод, я не разобралась





function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

//10
в функции переопределили исходный элемент массива





function a() {
    alert(this);
}
a.call(null);

//[object Window]
null - это объект, который содержит только одно значение - null.
По умолчанию this это объект window, его и получаем